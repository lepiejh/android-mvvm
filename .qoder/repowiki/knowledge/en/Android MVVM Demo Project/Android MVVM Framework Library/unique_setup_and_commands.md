Built as an Android library via Gradle (`mvvm/build.gradle`) with `dataBinding = true`; dependencies are resolved from the parent project's `rootProject.ext.support` and `rootProject.ext.dependencies` maps, so this module must be included as a subproject/module of a host app that defines those extensions. ProGuard rules are applied via `proguard-rules.pro` for debug/release builds.